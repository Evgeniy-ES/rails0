<tr>
  <td><%= question.body %></td>
  <td><%= question.test_id %></td>
  <td><%= link_to 'Show', admin_question_path(question) %></td>
  <td><%= link_to 'Edit', admin_edit_question_path(question) %></td>
  <td>
    <%= link_to 'Delete'
                 admin_question_path(question)
                 method: :delete,
                 data: { confirm: 'Are you sure?'} %>
  </td>
</tr>
